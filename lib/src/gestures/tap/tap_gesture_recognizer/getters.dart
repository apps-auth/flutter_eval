part of 'core.dart';

abstract class _InstanceDefaultPropsGetter
    implements InstanceDefaultPropsGetter<$TapGestureRecognizer> {}

// Getter onTapDown
class $TapGestureRecognizerGetterOnTapDown
    implements _InstanceDefaultPropsGetter {
  @override
  String get name => 'onTapDown';

  @override
  BridgeMethodDef get definition => const BridgeMethodDef(
        BridgeFunctionDef(
            returns: BridgeTypeAnnotation(BridgeTypeRef(CoreTypes.function),
                nullable: true)),
      );

  @override
  $Value? run(Runtime runtime, $TapGestureRecognizer target) {
    // onTapDown é um callback, retornamos null se não estiver definido
    return null;
  }
}

// Getter onTapUp
class $TapGestureRecognizerGetterOnTapUp
    implements _InstanceDefaultPropsGetter {
  @override
  String get name => 'onTapUp';

  @override
  BridgeMethodDef get definition => const BridgeMethodDef(
        BridgeFunctionDef(
            returns: BridgeTypeAnnotation(BridgeTypeRef(CoreTypes.function),
                nullable: true)),
      );

  @override
  $Value? run(Runtime runtime, $TapGestureRecognizer target) {
    // onTapUp é um callback, retornamos null se não estiver definido
    return null;
  }
}

// Getter onTap
class $TapGestureRecognizerGetterOnTap implements _InstanceDefaultPropsGetter {
  @override
  String get name => 'onTap';

  @override
  BridgeMethodDef get definition => const BridgeMethodDef(
        BridgeFunctionDef(
            returns: BridgeTypeAnnotation(BridgeTypeRef(CoreTypes.function),
                nullable: true)),
      );

  @override
  $Value? run(Runtime runtime, $TapGestureRecognizer target) {
    // onTap é um callback, retornamos null se não estiver definido
    return null;
  }
}

// Getter onTapCancel
class $TapGestureRecognizerGetterOnTapCancel
    implements _InstanceDefaultPropsGetter {
  @override
  String get name => 'onTapCancel';

  @override
  BridgeMethodDef get definition => const BridgeMethodDef(
        BridgeFunctionDef(
            returns: BridgeTypeAnnotation(BridgeTypeRef(CoreTypes.function),
                nullable: true)),
      );

  @override
  $Value? run(Runtime runtime, $TapGestureRecognizer target) {
    // onTapCancel é um callback, retornamos null se não estiver definido
    return null;
  }
}
