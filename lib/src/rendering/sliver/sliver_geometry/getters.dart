part of 'core.dart';

class $SliverGeometryGetterScrollExtent
    implements InstanceDefaultPropsGetter<$SliverGeometry> {
  @override
  String get name => 'scrollExtent';
  @override
  BridgeMethodDef get definition => const BridgeMethodDef(BridgeFunctionDef(
      returns: BridgeTypeAnnotation(BridgeTypeRef(CoreTypes.double))));
  @override
  $Value? run(Runtime runtime, $SliverGeometry target) => null;
}
